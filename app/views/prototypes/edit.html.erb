<div class="main">
  <div class="inner">
    <div class="form__wrapper">
      <h2 class="page-heading">プロトタイプ編集</h2>
        <%= form_with(model: @prototype, url: prototype_path(@prototype), local: true) do |f| %>
          <%= render partial: "form", locals: { f: f } %>
        <% end %>
    </div>
  </div>
</div>